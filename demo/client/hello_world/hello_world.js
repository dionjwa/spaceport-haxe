
function main(stage){
	console.log("called main");
	// // Loads hello world swf
	// var libraryLoader = new sp.Loader();
	// var libraryURL = "content/hello_world/Art/hello_world.swf"
	// var libraryURLRequest = new sp.URLRequest(libraryURL);
	// libraryLoader.load(libraryURLRequest);
	// stage.addChild(libraryLoader);
}
